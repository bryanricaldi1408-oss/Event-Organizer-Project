document.getElementById('loginForm').addEventListener('submit', function(e) {
    // HAPUS ATAU KOMENTARI SELURUH ISI FUNGSI INI!
    // JANGAN lakukan e.preventDefault()
    // JANGAN ada cek if(username === 'kapi' && password === 'owner123')
    // JANGAN ada window.location.href = 'dashboard.html'
    
    // Biarkan file ini kosong, atau hanya berisi fungsi utilitas jika ada.
    // Jika Anda ingin mempertahankan efek loading, Anda harus memodifikasinya 
    // agar tidak membatalkan submit form. Untuk saat ini, HAPUS SEMUA.
});